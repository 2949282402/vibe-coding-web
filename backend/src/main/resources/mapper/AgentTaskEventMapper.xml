<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hejulian.blog.agent.mapper.AgentTaskEventMapper">

    <resultMap id="AgentTaskEventResultMap" type="com.hejulian.blog.agent.entity.AgentTaskEvent">
        <id property="id" column="id"/>
        <result property="taskId" column="task_id"/>
        <result property="stepId" column="step_id"/>
        <result property="eventType" column="event_type"/>
        <result property="agentRole" column="agent_role"/>
        <result property="payloadJson" column="payload_json"/>
        <result property="payloadSummary" column="payload_summary"/>
        <result property="status" column="status"/>
        <result property="latencyMs" column="latency_ms"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="selectByTaskId" resultMap="AgentTaskEventResultMap">
        SELECT id, task_id, step_id, event_type, agent_role, payload_json, payload_summary, status, latency_ms, created_at
        FROM agent_task_event
        WHERE task_id = #{taskId}
        ORDER BY id ASC
    </select>

    <insert id="insert" parameterType="com.hejulian.blog.agent.entity.AgentTaskEvent">
        INSERT INTO agent_task_event (
            task_id, step_id, event_type, agent_role, payload_json, payload_summary, status, latency_ms, created_at
        )
        VALUES (
            #{taskId}, #{stepId}, #{eventType}, #{agentRole}, #{payloadJson}, #{payloadSummary}, #{status}, #{latencyMs}, CURRENT_TIMESTAMP
        )
    </insert>

    <insert id="insertErrorEvent">
        INSERT INTO agent_task_event (
            task_id, step_id, event_type, agent_role, payload_json, payload_summary, status, latency_ms, created_at
        )
        VALUES (
            #{taskId}, #{stepId}, #{eventType}, #{agentRole}, NULL, #{payloadSummary}, #{status}, #{latencyMs}, CURRENT_TIMESTAMP
        )
    </insert>

</mapper>

