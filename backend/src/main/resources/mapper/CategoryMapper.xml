<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hejulian.blog.mapper.CategoryMapper">

    <resultMap id="CategoryResultMap" type="com.hejulian.blog.entity.Category">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="slug" column="slug"/>
        <result property="description" column="description"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectAllOrderByName" resultMap="CategoryResultMap">
        SELECT id, name, slug, description, created_at, updated_at
        FROM categories
        ORDER BY name ASC
    </select>

    <select id="selectById" resultMap="CategoryResultMap">
        SELECT id, name, slug, description, created_at, updated_at
        FROM categories
        WHERE id = #{id}
        LIMIT 1
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(1) FROM categories
    </select>

    <select id="countById" resultType="long">
        SELECT COUNT(1) FROM categories WHERE id = #{id}
    </select>

    <select id="countBySlug" resultType="long">
        SELECT COUNT(1) FROM categories WHERE slug = #{slug}
    </select>

    <select id="countBySlugAndIdNot" resultType="long">
        SELECT COUNT(1) FROM categories WHERE slug = #{slug} AND id != #{id}
    </select>

    <insert id="insert" parameterType="com.hejulian.blog.entity.Category" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO categories (name, slug, description, created_at, updated_at)
        VALUES (#{name}, #{slug}, #{description}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <update id="update" parameterType="com.hejulian.blog.entity.Category">
        UPDATE categories
        SET name = #{name},
            slug = #{slug},
            description = #{description},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM categories WHERE id = #{id}
    </delete>

</mapper>

